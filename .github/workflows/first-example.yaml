name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v4

            - name: list and read files
              run: |
               echo "hello world"
               ls -ltra
               cat README.md
            - name: generate ASCII Artwork
              run: cowsay -f dragon "run for cow, i am a DRAGON" >> dragon.txt
                

          #  - name: list files
            #  run: ls

           # - name: read files
            #  run: cat README.md
              